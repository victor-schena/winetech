@model Admin.Models.ApplicationRole
<section class="content-header">
  <h1>
    Perfis
    <small>usuários cadastrados no perfil [@ViewBag.Role]</small>
  </h1>
</section>
<section class="content">
  <div class="row">
    <div class="col-lg-12">
      <div class="box box-primary">
        <div class="box-body">
          <div class="table">
            <table class="table table-striped table-bordered table-hover">
              <thead>
                <tr>
                  <th>Usuário</th>
                </tr>
              </thead>
              <tbody>
                @foreach (var item in ViewBag.Users)
                {
                  <tr class="odd gradeX">
                    <td>
                      @item.UserName
                    </td>
                  </tr>
                }
              </tbody>
            </table>
          </div>
        </div>
        <div class="box-footer">
          <a class="btn btn-danger btn-flat" href="@Url.Action("Index", "Roles")" role="button" title="Voltar">Voltar</a>
        </div>
      </div>
    </div>
  </div>
</section>
<script>
  setactivemenu('#a-roles');
</script>
